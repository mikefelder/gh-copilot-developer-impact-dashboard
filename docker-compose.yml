# GITHUB_PAT=ghp_xxxxxxx docker compose up --build
version: '3.8'

services:
  elasticsearch:
    build:
      context: ./src/elasticsearch
    container_name: elasticsearch
    ports:
      - "9200:9200"
    volumes:
      - data:/usr/share/elasticsearch/data
      - logs:/usr/share/elasticsearch/logs
    mem_limit: 1g
    cpus: 1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200/_cluster/health/"]
      interval: 30s
      timeout: 10s
      retries: 5

  grafana:
    build:
      context: ./src/grafana
    container_name: grafana
    mem_limit: 1g
    cpus: 0.5
    ports:
      - "8080:3000"
    depends_on:
      - elasticsearch
    volumes:
      - grafana:/var/lib/grafana
      - ./grafana-provisioning/dashboards:/var/lib/grafana/dashboards
      - ./grafana-provisioning/dashboard.yaml:/etc/grafana/provisioning/dashboards/dashboard.yaml
      - ./user_advance_metrics_dashboard.json:/var/lib/grafana/dashboards/user_advance_metrics_dashboard.json
    environment:
      GF_LOG_LEVEL: debug
      GF_SERVER_HTTP_PORT: 3000
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: copilot
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  init-grafana:
    mem_limit: 0.5g
    cpus: 0.25
    build:
      context: .
      dockerfile: ./src/cpuad-updater/grafana/Dockerfile
    container_name: init-grafana
    depends_on:
      - grafana
      - elasticsearch
      - cpuad-updater
    environment:
      ELASTICSEARCH_URL: http://elasticsearch:9200
      GRAFANA_URL: http://grafana:3000
      GRAFANA_USERNAME: admin
      GRAFANA_PASSWORD: copilot
    restart: "no"

  cpuad-updater:
    mem_limit: 0.5g
    cpus: 0.25
    build:
      context: ./src/cpuad-updater
    container_name: cpuad-updater
    depends_on:
      - elasticsearch
    environment:
      GITHUB_PAT: ${GITHUB_PAT:-}
      ORGANIZATION_SLUGS: ${ORGANIZATION_SLUGS:-standalone:octodemo-copilot-standalone}
      ELASTICSEARCH_URL: http://elasticsearch:9200
      INDEX_USER_METRICS: copilot_user_metrics
      INDEX_USER_ADOPTION: copilot_user_adoption
      INDEX_DEVELOPER_ACTIVITY: developer_activity
      ENABLE_DEVELOPER_ACTIVITY: ${ENABLE_DEVELOPER_ACTIVITY:-true}
      DEVELOPER_ACTIVITY_DAYS_BACK: ${DEVELOPER_ACTIVITY_DAYS_BACK:-28}
      EXECUTION_INTERVAL_HOURS: ${EXECUTION_INTERVAL_HOURS:-1}
      ENABLE_DEMO_MODE: ${ENABLE_DEMO_MODE:-false}
      # Add other environment variables as needed
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "pgrep", "-f", "python"]
      interval: 5m
      timeout: 10s
      retries: 3

volumes:
  data:
  logs:
  grafana: